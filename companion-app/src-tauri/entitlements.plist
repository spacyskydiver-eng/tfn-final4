<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  macOS entitlements for ROK Companion.

  Permissions used:
    com.apple.security.app-sandbox         — required for App Store distribution
    com.apple.security.network.client      — outbound HTTP to submit events to backend
    com.apple.security.files.user-selected.read-only — not required but harmless

  Clipboard access (NSPasteboardType) does NOT require a special entitlement —
  macOS prompts the user once via TCC when the app first reads from the clipboard.
-->
<plist version="1.0">
<dict>
  <!-- Sandbox (required for notarisation + App Store) -->
  <key>com.apple.security.app-sandbox</key>
  <true/>

  <!-- Allow outbound network connections to submit events to the backend -->
  <key>com.apple.security.network.client</key>
  <true/>

  <!--
    Screen Recording (ScreenCaptureKit).
    The actual permission is a TCC prompt the user sees on first launch.
    This entitlement is required to allow the hardened runtime to use
    ScreenCaptureKit at all within the sandbox.
  -->
  <key>com.apple.security.screen-recording</key>
  <true/>
</dict>
</plist>
